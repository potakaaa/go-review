import Image from "next/image";
import Link from "next/link";
import { PublicCta, PublicFooter, PublicHeader } from "@/components/public-site";

export function PlatformTapCardPage({ platform, image, destination, accent }: { platform: "Facebook" | "Instagram"; image: string; destination: string; accent: string }) {
  return <><PublicHeader /><main><section className="mx-auto grid min-h-[650px] w-[calc(100%-2.5rem)] max-w-[1180px] items-center gap-10 py-14 md:grid-cols-2 md:py-20"><div><p className="eyebrow">{platform} tap card</p><h1 className="mt-5 text-[clamp(3.2rem,8vw,6.5rem)] leading-[.9] tracking-[-.07em]">Make your {platform} <span className="display-heading text-muted">one tap away.</span></h1><p className="mt-6 max-w-xl text-base leading-8 text-muted">A 3×4-inch adhesive NFC and QR card that opens {destination}. Stick it on a wall, table, counter, or anywhere customers naturally pause.</p><div className="mt-8 flex items-center gap-5"><Link href="/order" className="inline-flex min-h-12 items-center rounded-lg bg-ink px-6 py-3 text-sm font-semibold text-canvas">Order {platform} cards ↗</Link><strong className="text-xl">₱299 each</strong></div><p className="mt-4 text-xs text-subtle">Minimum 3 tap cards total · Mix platforms · Free delivery within CDO</p></div><Image src={image} alt={`${platform} NFC and QR tap card held in a hand`} width={1254} height={1254} priority className="w-full rounded-2xl" /></section><section className="border-y border-line bg-surface py-16"><div className="mx-auto grid w-[calc(100%-2.5rem)] max-w-[1000px] gap-8 md:grid-cols-3">{[["Tap or scan","Works with NFC-enabled phones and every camera that scans QR codes."],["Direct destination",`Opens ${destination} without asking customers to search.`],["Editable later","The printed Goreview link stays fixed while authorized staff can update its destination."]].map(([title,copy]) => <article key={title}><p className="eyebrow" style={{ color: accent }}>{title}</p><p className="mt-4 text-sm leading-7 text-muted">{copy}</p></article>)}</div></section></main><PublicCta title={`Bring your ${platform} closer.`} description="Choose your quantities and mix platforms in one simple order inquiry." /><PublicFooter /></>;
}
